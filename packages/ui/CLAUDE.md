# packages/ui — Reusable Svelte components

- Svelte 5 runes syntax only — no legacy `$:` reactivity
- Components are shared across routes in `apps/web`
- Depends on `@sg/contracts` for typed props where needed
- No Phaser types. No game logic. Pure UI components.
- Export all public components from `src/index.ts`
